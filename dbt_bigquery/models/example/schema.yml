
version: 2

models:
    - name: my_first_dbt_model
      description: "A starter dbt model"
      columns:
          - name: id
            description: "The primary key for this table"
            tests:
                - unique
                - not_null

    - name: my_second_dbt_model
      description: "A starter dbt model"
      columns:
          - name: id
            description: "The primary key for this table"
            tests:
                - unique
                - not_null

    - name: sample_data
      columns:
          - name: generic_id
            tests:
                - is_unique_custom



exposures:
  - name: core
    type: application
    depends_on:
      - ref('some_new_model_19')
    owner:
      name: "Scott Florence"
      email: scott.florence@macquarie.com
    meta:
      workload_access:
        aws:
          - "arn:aws:ec2:ap-southeast-2:269421376940:instance/i-0f10a3c364f2be6df"
